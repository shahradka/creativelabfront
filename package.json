{
  "name": "creativelabfront",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:web": "turbo build --filter=web",
    "build:storybook": "turbo build --filter=storybook",
    "dev:storybook": "turbo dev --filter=storybook",
    "dev": "turbo dev --filter=web",
    "start": "turbo dev --parallel --filter=storybook --filter=web",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@creativelabfront/eslint-config": "workspace:*",
    "@creativelabfront/typescript-config": "workspace:*",
    "prettier": "^3.7.4",
    "tailwindcss": "^4.1.18",
    "turbo": "^2.6.3",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
